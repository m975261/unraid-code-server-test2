# Unraid Templates for code-server-test2

XML templates for Unraid Community Applications.

## Install
Copy `my-code-server-test2.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
